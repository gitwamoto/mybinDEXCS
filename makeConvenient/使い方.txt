(0) open_in_terminal.shとresoucesフォルダを同じフォルダ内に置く．
(1) open_in_terminal.shをダブルクリック，「端末内で実行する」を選ぶ．
    単にテキストエディタが起動する場合，実行権限がなくなってしまっている．
    この場合，open_in_terminal.shを右クリック，「プロパティ」タブの「プログラムとして実行可能」をチェックする．
(2) パスワードを入力後，Enterキーを押す．
